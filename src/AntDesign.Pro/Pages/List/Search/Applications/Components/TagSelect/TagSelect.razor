@namespace AntDesign.Pro.Template.Pages.List
@inherits AntDomComponentBase

<div class="@ClassMapper.Class" style="@Style">
    @if (!HideCheckAll)
    {
        <Tag Checkable Checked="_checkedAll" CheckedChanged="HandleCheckedChange">@SelectAllText</Tag>
    }
    
    <CascadingValue Value="this">
        @ChildContent
    </CascadingValue>

    @if (Expandable)
    {
        <a class="trigger" @onclick="HandleExpand">
            @if (_expand)
            {
                @CollapseText <Icon Type="Up" Theme="@IconThemeType.Outline" />
            }
            else
            {
                @ExpandText <Icon Type="Down" Theme="@IconThemeType.Outline" />
            }
        </a>
    }
</div>